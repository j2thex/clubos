# Copy to .env.test.local. Values come from `pnpm supabase start`.
# Tests run against the local Supabase instance only — never staging or prod.
SUPABASE_TEST_URL=http://127.0.0.1:54321
SUPABASE_TEST_SERVICE_ROLE=
